<%= form_for(city) do |f| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Имя' %>
    <%= f.text_field :name %>
  </div>

    <div class="field">
      <%= f.label 'Индекс' %>
      <%= f.text_field :zip_code %>
    </div>

    <div class="field">
      <%= f.label 'ID города' %>
      <%= f.text_field :city_id %>
    </div>

    <div class="field">
      <%= f.label 'Координаты (Latitude, Longitude)' %>
      <%= f.text_field :lat %>, <%= f.text_field :lng %>
    </div>

  <div class="actions">
    <%= f.submit 'Сохранить' %>
  </div>
<% end %>
